// when you use the cart from the context, you will need to check for whether size and color are an empty string or not to determine whether to render the accompanying elements
// i also realized I am only giving the cart the index in the products json and not info like the price or name, so that'll all have to happen here too

const CartPage = () => {
    return (<></>);
}
export default CartPage;
